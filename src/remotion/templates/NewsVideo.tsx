/**
 * NewsVideo template — vertical 1080x1920 news short.
 * Showcase version: skeleton only.
 */

import { AbsoluteFill, Sequence } from 'remotion';

export const NewsVideo: React.FC<{ headlines: string[] }> = ({ headlines }) => {
  return (
    <AbsoluteFill style={{ backgroundColor: '#0a0a0a' }}>
      {/* [Title sequence not shown] */}
      {/* [Per-headline animated card not shown] */}
      {/* [Outro CTA not shown] */}
    </AbsoluteFill>
  );
};
